// The page's face, so the site reads like the product it describes. A client module rather than a
// CSS `@import` of a package path, whose resolution depends on the loader.
import '@fontsource-variable/inter';
