---
---

The web page shows the server's version in its header, and a banner on every page when a newer release is out, with the commands that install it: `docker compose pull && docker compose up -d` for the image, noting that a compose file pinning `RHAPSODE_VERSION` needs it changed first, and the checkout's steps otherwise.
