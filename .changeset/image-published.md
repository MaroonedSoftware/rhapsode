---
'rhapsode': minor
---

CI publishes the Docker image to `ghcr.io/maroonedsoftware/rhapsode` for amd64 and arm64, after the build and the Docker smoke test pass: `edge` and `sha-<commit>` from `main`, and `1.2.3`, `1.2` and `latest` from a `v1.2.3` tag.
