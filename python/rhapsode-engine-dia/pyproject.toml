[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rhapsode-engine-dia"
version = "0.1.2"
description = "Dia for rhapsode: two speakers in one pass, and nonverbals written where they happen."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "rhapsode-worker==0.1.2",  # Its own version, exactly, and bumped with it. protocol.md § 9.
]

[tool.hatch.build.targets.wheel]
packages = ["src/rhapsode_engine_dia"]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
strict = true
files = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
timeout = 60
